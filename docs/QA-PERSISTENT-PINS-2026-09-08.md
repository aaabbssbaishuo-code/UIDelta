# 已记录问题标记 · 2026-09-08

## 交互评审

按 emil-design-eng 的视觉居中、原生操作与克制反馈原则调整，保留现有品牌橙色。

| Before | After | Why |
| --- | --- | --- |
| 浏览模式清空并隐藏全部标记 | 浏览、标注、UI 和清单都保留当前页面可定位的问题编号 | 浏览时能识别已经反馈的位置 |
| 按钮默认内边距挤压小圆圈 | 零内边距、网格居中、等宽数字、白描边；多位数等比增大圆圈 | 数字清楚且居中，不撑成椭圆 |
| 每次刷新销毁所有标记 | 按问题 ID 复用节点，同位置编号尝试错开 | 减少闪烁，避免常见重叠 |
| 浏览标记可能阻挡原网页 | 浏览和临时交互模式下点击穿透、不占用 Tab | 不影响原网页开关、链接与表单 |
| 浏览时不更新标记 | 滚动、布局变化和路由监听均更新；隐藏 / 无法定位的目标不显示 | 避免停留在旧视口位置或其他页面 |

## 验证

- 自动测试 173 项通过，包括新增 4 项标记回归：模式显示、复用与移动、重叠与关闭、框选文档坐标换算。
- 独立 headless Chrome：浏览 / 标注 / UI 显示，1 / 16 / 128 的圆形尺寸与数字居中，点击穿透，滚动跟随，隐藏目标、路由过滤通过。已有焦点、弹窗和粘贴回归也通过。
- 实际查看 `.qa/persistent-pins.png`：橙色圆点、白描边、数字居中；未添加持续闪烁动画。
- 语法、文件一致性与 diff 空白检查通过。

未读取、清空、重排或修改用户当前走查数据；未重载扩展或刷新用户网页。测试使用虚构问题和独立浏览器。源码已同步至原加载目录，用户保存好当前工作后可自行重载。

边界：无法定位或已隐藏的目标不伪造位置；自由框选按记录时的文档坐标定位。未复测用户真实 Womate 页面；极密集标记或虚拟列表复用仍需按真实页面验收。
