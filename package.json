{
  "name": "uidelta",
  "version": "0.9.4",
  "private": true,
  "description": "Open-source UI inspection, issue capture and evidence handoff for designers.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "check:installed": "node scripts/check-installed.mjs",
    "test": "node --test extension/*.test.mjs",
    "build": "node scripts/build.mjs"
  }
}
